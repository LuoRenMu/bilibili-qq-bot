{
  "convert_messages": [
    {
      "id": "bilibili_article",
      "format_messages": "${up_name}Update new content\n${origin_text}\nBy OneBot-LoMu"
    },
    {
      "id": "forward_text",
      "format_messages": "${origin_text}"
    },
    {
      "id": "forward_image",
      "format_messages": "${origin_image}"
    },
    {
      "id": "live_push",
      "format_messages": "${up_name} 正在直播中${live_title}  ${live_cover}"
    }
  ]
}